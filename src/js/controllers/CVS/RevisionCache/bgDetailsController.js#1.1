app.controller("bgDetailsCtrl", function ($scope,$stateParams,vertxEventBusService,$state,actionData) {
    console.log("enta en bgDetailsCtrl");

           if(sessionStorage.logado===undefined)
                 {
                     $state.go("Login");
                 }
                 else{
                       console.log("enta en bgDetailsCtrl");
                       
                        $scope.$on('numBG', function(ev, args) {
                         
                         
                         $scope.totalBindGroups = args.numBG;
                         
                         console.log("totalBindGroups "+ $scope.totalBindGroups)
//                          $scope.numvalues = [3,5,10,20,50];
//                        $scope.currentPage = 1;
//                        
//                        $scope.maxRangeBG = ($scope.currentPage * $scope.itemsPerPageBGdetails);
//                        $scope.minRangeBG = ($scope.maxRangeBG-$scope.itemsPerPageBGdetails)+1;
//                        if($scope.totalBindGroups < $scope.maxRangeBG){
//                                $scope.maxRangeBG = $scope.totalBindGroups;
//                            }
//                        $scope.refreshItemsPerPage = function(new_value){
//                            $scope.itemsPerPageBGdetails = new_value;
//                        };
//                        
//                        console.log("maxRangeBG vale: " +  $scope.maxRangeBG);    
//                        
//                         $scope.setRange= function(currentPage){
//                            $scope.currentPage = currentPage;
//                            sessionStorage.currentPage = $scope.currentPage;
//                            $scope.maxRangeBG = ($scope.currentPage * $scope.itemsPerPageBGdetails);
//                            $scope.minRangeBG = ($scope.maxRangeBG-$scope.itemsPerPageBGdetails)+1;
//                            if($scope.totalBindGroups < $scope.maxRangeBG){
//                                $scope.maxRangeBG = $scope.total;
//                            }
//                         };
                     });
                    }
                
});
